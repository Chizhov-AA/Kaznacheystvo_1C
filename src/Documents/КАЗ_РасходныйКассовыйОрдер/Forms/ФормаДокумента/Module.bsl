&НаКлиенте
Процедура ЗаявкаПриИзменении(Элемент)

	Если Не ЗначениеЗаполнено(Объект.Заявка) Тогда
		Возврат;
	КонецЕсли;

	Объект.Сумма = СуммаЗаявкиНаСервере(Объект.Заявка);

КонецПроцедуры

&НаСервереБезКонтекста
Функция СуммаЗаявкиНаСервере(Заявка)

	Возврат Заявка.СуммаДокумента;

КонецФункции
